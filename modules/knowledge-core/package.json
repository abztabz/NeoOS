{
  "name": "@neoos/knowledge-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared governed knowledge ingestion and retrieval core for NeoOS.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
